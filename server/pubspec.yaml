name: tlw_control_plane
description: >-
  Control plane per il provisioning outbound di nodi Lightning self-hosted
  (bitcoin-blake2b): registro token/nodi + admin CLI. Non custodisce fondi.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  args: ^2.5.0
  crypto: ^3.0.3
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  sqlite3: ^2.4.6

dev_dependencies:
  http: ^1.3.0
  lints: ^5.0.0
  test: ^1.25.0
